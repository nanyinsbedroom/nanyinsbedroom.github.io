# Node.js dependencies
# This folder contains all the packages your project needs, which can be reinstalled with `npm install`.
# It is very large and should never be uploaded.
node_modules

# Next.js build and cache folders
# These are generated automatically when you run or build your app.
.next
out

# Environment variables
# IMPORTANT: These files can contain secrets like API keys. Never commit them to GitHub.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Log files generated by npm or other tools
npm-debug.log*
yarn-error.log*
yarn-debug.log*

# Editor configuration folders
# These are specific to your personal editor setup.
.vscode
.idea

# Operating System generated files
.DS_Store
Thumbs.db